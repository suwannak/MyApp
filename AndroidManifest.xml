<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acreagefarmerapponandroid">

    <!-- Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        //Login
        <activity android:name=".LoginActivity" />
        ///About
        <activity
            android:name=".AboutActivity"
            android:label="เกี่ยวกับแอปพลิเคชั่น"
            android:theme="@style/AppTheme" />
        ///LeaderMenu
        <activity android:name=".LeaderMenuActivity" />
        ////FarmerMenu
        <activity android:name=".FarmerMenuActivity" />
        ///Insert1
        <activity android:name=".Insert1Activity" />
        <activity android:name=".Insert2Activity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ReadActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UserActivity"></activity>
    </application>

</manifest>